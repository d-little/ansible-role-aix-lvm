---
# defaults file for SHUSA ansible_role_soe_storage.
# Please do not change these values, use your local environment variables files instead

# Defines if LVM will be managed by role
# default is false to ensure nothing is changed by accident.
manage_lvm: false

##
storage_defaults:
  force: false
  fs: jfs2